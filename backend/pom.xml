<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.18</version>
        <relativePath/>
    </parent>

    <groupId>com.luxera</groupId>
    <artifactId>companion-platform-backend</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>companion-platform-backend (parent)</name>
    <description>LAP v1 parent — contracts / platform-kernel / chat-platform / digital-human-platform / application-platform / bootstrap-app</description>

    <properties>
        <java.version>17</java.version>
        <jjwt.version>0.11.5</jjwt.version>
        <archunit.version>1.0.1</archunit.version>
    </properties>

    <modules>
        <module>contracts</module>
        <module>platform-kernel</module>
        <module>chat-platform</module>
        <module>digital-human-platform</module>
        <module>application-platform</module>
        <module>bootstrap-app</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.luxera</groupId>
                <artifactId>companion-platform-contracts</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.luxera</groupId>
                <artifactId>companion-platform-kernel</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.luxera</groupId>
                <artifactId>companion-platform-chat</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.luxera</groupId>
                <artifactId>companion-platform-digital-human</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.luxera</groupId>
                <artifactId>companion-platform-application</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
